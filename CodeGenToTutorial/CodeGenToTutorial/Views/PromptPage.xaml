<Page
    x:Class="CodeGenToTutorial.Views.PromptPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>

        <TextBlock
            x:Uid="Prompt_Header"
            Grid.Row="0"
            Style="{ThemeResource SubtitleTextBlockStyle}" />

        <Grid
            Grid.Row="1"
            Margin="{StaticResource SmallTopMargin}"
            ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox
                x:Uid="Prompt_WorkingDirectoryTextBox"
                Grid.Column="0"
                Text="{x:Bind ViewModel.WorkingDirectoryPath, Mode=OneWay}"
                IsReadOnly="True" />
            <Button
                x:Uid="Prompt_BrowseButton"
                Grid.Column="1"
                VerticalAlignment="Bottom"
                Command="{x:Bind ViewModel.BrowseWorkingDirectoryCommand}" />
        </Grid>

        <TextBox
            x:Name="PromptTextBox"
            x:Uid="Prompt_TextBox"
            Grid.Row="2"
            Margin="{StaticResource SmallTopMargin}"
            Text="{x:Bind ViewModel.PromptText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            AcceptsReturn="True"
            TextWrapping="Wrap"
            ScrollViewer.VerticalScrollBarVisibility="Auto" />

        <StackPanel
            Grid.Row="3"
            Margin="{StaticResource SmallTopMargin}"
            Orientation="Horizontal"
            Spacing="8">
            <Button
                x:Uid="Prompt_SaveButton"
                Command="{x:Bind ViewModel.SavePromptCommand}" />
            <Button
                x:Uid="Prompt_RunButton"
                Command="{x:Bind ViewModel.RunPromptCommand}"
                Style="{StaticResource AccentButtonStyle}" />
            <ProgressRing
                Width="20"
                Height="20"
                VerticalAlignment="Center"
                IsActive="{x:Bind ViewModel.IsRunning, Mode=OneWay}" />
        </StackPanel>

        <TextBlock
            Grid.Row="4"
            Margin="{StaticResource XSmallTopMargin}"
            Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" />

        <TextBlock
            x:Uid="Prompt_OutputHeader"
            Grid.Row="5"
            Margin="{StaticResource SmallTopMargin}"
            Style="{ThemeResource BodyStrongTextBlockStyle}" />

        <TextBox
            Grid.Row="6"
            Margin="{StaticResource XSmallTopMargin}"
            Text="{x:Bind ViewModel.CliOutput, Mode=OneWay}"
            IsReadOnly="True"
            AcceptsReturn="True"
            TextWrapping="Wrap"
            FontFamily="Cascadia Mono"
            ScrollViewer.VerticalScrollBarVisibility="Auto" />
    </Grid>
</Page>
